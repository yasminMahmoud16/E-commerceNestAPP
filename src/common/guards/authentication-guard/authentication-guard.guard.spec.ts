import { AuthenticationGuardGuard } from './authentication-guard.guard';

describe('AuthenticationGuardGuard', () => {
  it('should be defined', () => {
    expect(new AuthenticationGuardGuard()).toBeDefined();
  });
});
