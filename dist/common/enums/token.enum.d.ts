export declare enum SignatureLevelEnum {
    Bearer = "Bearer",
    System = "System"
}
export declare enum TokenEnum {
    access = "access",
    refresh = "refresh"
}
export declare enum LogoutEnum {
    only = "only",
    all = "all"
}
